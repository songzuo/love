services:
  - type: web
    name: dating-app-api
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && npm run build && npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000